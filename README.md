# Express with Socket.io Starter
A barebones starter for using Socket.io with Express.

### Set Up

```shell
git clone https://github.com/PandemicMoon/express-socketio-starter.git myApp
cd myApp && npm install
npm start
```
